{
  "name": "auth-google",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "doppler run -- cypress open",
    "test": "HTTP_PROXY=$(echo STATIC_IP_HTTP_PROXY) cypress run --browser chrome"
  },
  "dependencies": {
    "cypress": "^9.1.0"
  }
}
