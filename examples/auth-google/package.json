{
  "name": "auth-google",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "doppler run -- cypress open",
    "test": "export HTTP_PROXY=$STATIC_IP_HTTP_PROXY cypress run --browser chrome"
  },
  "dependencies": {
    "cypress": "^9.1.0"
  }
}
