{
  "name": "sitemap",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "doppler run -- cypress open",
    "test": "cypress run"
  },
  "dependencies": {
    "cypress": "^8.3.1"
  }
}
