{
  "name": "gmail",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "doppler run -- cypress open",
    "test": "cypress run",
    "postsetup": "yarn ts-node ./cypress/plugins/refreshGmailToken"
  },
  "dependencies": {
    "cypress": "^8.5.0",
    "gmail-tester": "^1.3.3"
  }
}
