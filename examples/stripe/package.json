{
  "name": "stripe",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cypress open",
    "test": "cypress run --browser chrome"
  },
  "dependencies": {
    "cypress": "^10.7.0",
    "cypress-plugin-stripe-elements": "^1.0.2"
  }
}
