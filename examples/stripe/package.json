{
  "name": "stripe",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cypress open",
    "test": "echo 'no tests from stripe due to github runner blocked by stripe'",
    "test:real": "cypress run --browser chrome"
  },
  "dependencies": {
    "cypress": "^14.3.2",
    "cypress-plugin-stripe-elements": "^1.0.2"
  }
}
